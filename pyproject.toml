[project]
name = "messmind"
version = "0.1.0"
description = "A student meal-planning app for estimating mess attendance."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115,<1.0",
    "python-dotenv>=1.0,<2.0",
    "uvicorn[standard]>=0.30,<1.0",
]

[tool.vercel]
entrypoint = "backend.main:app"
